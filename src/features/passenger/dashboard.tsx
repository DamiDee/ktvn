/**
 * Superseded by src/features/passenger/rides-list.tsx.
 *
 * Passengers now land on their rides, where requesting sits at the top. This
 * file is no longer imported anywhere and can be deleted; it is left as an
 * empty module so an old copy of it cannot break the build.
 */
export {};
